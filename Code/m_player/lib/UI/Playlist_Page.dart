import 'package:flutter/material.dart';

class Playlist_Page extends StatelessWidget {
  const Playlist_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
