import 'package:flutter/material.dart';

class Downloads_Page extends StatelessWidget {
  const Downloads_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
